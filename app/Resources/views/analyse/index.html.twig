{% extends 'layout.html.twig' %}

{% block appWindow %}
        {{ block('analyse_creation') }}
{% endblock %}

{% block analyse_creation %}
    <div class="col-lg-12" id="mainWindow">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}